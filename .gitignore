# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
*.egg

# 运行时数据
data/db.sqlite3
data/logs/

# 环境变量（含敏感信息）
.env
.env.prod

# Lagrange 运行时文件（QQ 登录凭证 + 二进制）
lagrange/Keystore
lagrange/*.zip
lagrange/*.exe
lagrange/*.dll
lagrange/*.so
lagrange/*.dylib
lagrange/*.log
# 保留 appsettings.json 模板
!lagrange/appsettings.json
!lagrange/appsettings.*.json

# RSSHub（独立 git 仓库，本地手动管理）
rsshub/

# IDE
.idea/
.vscode/
*.swp
